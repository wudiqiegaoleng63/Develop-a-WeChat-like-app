# 教学文档 12: 群组进群与退群

## 本章完成的接口

| 接口 | 路由 | 功能描述 |
|------|------|---------|
| `EnterGroupDirectly` | `/group/enterGroupDirectly` | 直接进群（无需审核） |
| `LeaveGroup` | `/group/leaveGroup` | 退出群聊 |
| `DismissGroup` | `/group/dismissGroup` | 解散群聊（群主） |

### 开发顺序（重要）

```
Model → DAO → Service → Controller → 路由注册

① Model层: 群组模型已定义（group_info.go）
② DAO层: 数据库连接已在前序文档完成
③ Service层: 进群/退群业务逻辑实现（group_info_service.go）
④ Controller层: HTTP请求处理（group_info_controller.go）
⑤ 路由注册: 在https_server中注册路由
```

### 导入路径说明（重要）

Service层代码需导入以下包：

```go
import (
    "encoding/json"
    "errors"
    "time"
    "gorm.io/gorm"
    
    "gochat/internal/dao"
    "gochat/internal/model"
    "gochat/pkg/constants"
    "gochat/pkg/zlog"
    
    // ★枚举导入路径需包含中间层级
    "gochat/pkg/enum/contact/contact_type_enum"
    "gochat/pkg/enum/contact/contact_status_enum"
    
    // ★Redis服务
    myredis "gochat/internal/service/redis"
)
```

本文档详细介绍群组相关的三个核心接口实现：直接进群、离开群聊和解散群聊。

---

## 一、POST /group/enterGroupDirectly - 直接进群

### 1.1 接口概述

当用户申请加入群聊时，如果群聊的加群方式（AddMode）设置为"直接加入"，前端会调用此接口将用户直接添加到群组中，无需群主审核。

**核心功能：**
- 将用户UUID添加到群组成员列表（Members字段）
- 更新群成员数量（MemberCnt + 1）
- 为新成员创建用户联系人记录（UserContact表）

**关键技术点：** JSON反序列化、append添加成员、JSON序列化

---

### 1.2 Request结构体定义

```go
// EnterGroupDirectlyRequest 直接进群请求
type EnterGroupDirectlyRequest struct {
    OwnerId   string `json:"owner_id"`   // 群聊ID
    ContactId string `json:"contact_id"` // 新成员用户ID
}
```

**字段说明：**
- `OwnerId`：群聊的唯一标识UUID（注意：参数名为OwnerId，但实际传的是群聊ID）
- `ContactId`：要加入群聊的用户UUID

---

### 1.3 Respond结构体定义

此接口无特殊返回数据，仅返回操作结果。

```go
// 标准响应
{
    "code": 0,           // 0表示成功，-1表示系统错误
    "message": "进群成功" // 或错误信息
}
```

---

### 1.4 Controller层完整代码

```go
// api/v1/group_info_controller.go
// EnterGroupDirectly 直接进群
func EnterGroupDirectly(c *gin.Context) {
    var req request.EnterGroupDirectlyRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.EnterGroupDirectly(req.OwnerId, req.ContactId)
    JsonBack(c, message, ret, nil)
}
```

**代码解析：**
1. 使用`BindJSON`解析请求参数
2. 参数校验失败时返回500错误
3. 调用Service层处理业务逻辑
4. 使用`JsonBack`统一返回响应

---

### 1.5 Service层完整代码

```go
// internal/service/gorm/group_info_service.go
// EnterGroupDirectly 直接进群
// ownerId 是群聊id
func (g *groupInfoService) EnterGroupDirectly(ownerId, contactId string) (string, int) {
    // 1. 查询群聊信息
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", ownerId); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 2. JSON反序列化：将Members字段从JSON转换为[]string切片
    var members []string
    if err := json.Unmarshal(group.Members, &members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 3. 添加新成员到切片
    members = append(members, contactId)

    // 4. JSON序列化：将切片转换回JSON格式
    if data, err := json.Marshal(members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    } else {
        group.Members = data
    }

    // 5. 更新群成员数量
    group.MemberCnt += 1

    // 6. 保存群聊信息
    if res := dao.GormDB.Save(&group); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 7. 创建用户联系人记录
    newContact := model.UserContact{
        UserId:      contactId,
        ContactId:   ownerId,
        ContactType: contact_type_enum.GROUP,    // 群聊类型
        Status:      contact_status_enum.NORMAL, // 正常状态
        CreatedAt:   time.Now(),
        UpdateAt:    time.Now(),
    }
    if res := dao.GormDB.Create(&newContact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 8. ★清除Redis缓存（带错误处理）
	if err := myredis.DelKeysWithPattern("group_session_list_" + ownerId); err != nil {
		zlog.Error(err.Error())
	}
	if err := myredis.DelKeysWithPattern("my_joined_group_list_" + ownerId); err != nil {
		zlog.Error(err.Error())
	}

	return "进群成功", 0
}
```

---

### 1.6 数据库操作说明

| 操作 | 表 | 说明 |
|------|-----|------|
| SELECT | GroupInfo | 根据群聊UUID查询群聊信息 |
| UPDATE | GroupInfo | 更新Members字段和MemberCnt字段 |
| INSERT | UserContact | 创建新的用户联系人记录 |

---

### 1.7 关键概念详解

#### JSON反序列化与序列化

在Go语言中，数据库中的JSON类型字段通常使用`json.RawMessage`类型存储。要进行操作，需要先反序列化为Go的切片类型。

**数据库模型定义：**
```go
type GroupInfo struct {
    // ...
    Members   json.RawMessage `gorm:"column:members;type:json;comment:群组成员"`
    // ...
}
```

**反序列化（JSON -> Go切片）：**
```go
var members []string
if err := json.Unmarshal(group.Members, &members); err != nil {
    // 错误处理
}
// 此时 members = ["user_uuid_1", "user_uuid_2", ...]
```

**序列化（Go切片 -> JSON）：**
```go
if data, err := json.Marshal(members); err != nil {
    // 错误处理
} else {
    group.Members = data
}
// 此时 group.Members = []byte(`["user_uuid_1","user_uuid_2",...]`)
```

#### append添加成员

Go语言的`append`函数用于向切片追加元素：

```go
// append(原切片, 新元素...)
members = append(members, contactId)
```

**特点：**
- `append`返回一个新的切片（可能指向新分配的内存）
- 必须将返回值赋值回原变量
- 如果容量不足，会自动扩容

---

## 二、POST /group/leaveGroup - 离开群聊

### 2.1 接口概述

用户主动退出群聊时调用此接口。该操作会：
- 从群成员列表中移除该用户
- 删除用户与群聊的会话记录
- 删除用户与群聊的联系人记录
- 删除用户向群聊发起的申请记录

**关键技术点：** slice删除元素（append(members[:i], members[i+1:]...)）

---

### 2.2 Request结构体定义

```go
// LeaveGroupRequest 退群请求
type LeaveGroupRequest struct {
    UserId  string `json:"user_id"`  // 用户ID
    GroupId string `json:"group_id"` // 群聊ID
}
```

**字段说明：**
- `UserId`：要退群的用户UUID
- `GroupId`：群聊的唯一标识UUID

---

### 2.3 Respond结构体定义

此接口无特殊返回数据，仅返回操作结果。

```go
// 标准响应
{
    "code": 0,           // 0表示成功，-1表示系统错误
    "message": "退群成功" // 或错误信息
}
```

---

### 2.4 Controller层完整代码

```go
// api/v1/group_info_controller.go
// LeaveGroup 退群
func LeaveGroup(c *gin.Context) {
    var req request.LeaveGroupRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.LeaveGroup(req.UserId, req.GroupId)
    JsonBack(c, message, ret, nil)
}
```

---

### 2.5 Service层完整代码

```go
// internal/service/gorm/group_info_service.go
// LeaveGroup 退群
func (g *groupInfoService) LeaveGroup(userId string, groupId string) (string, int) {
    // 1. 查询群聊信息
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", groupId); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 2. JSON反序列化：将Members字段转换为切片
    var members []string
    if err := json.Unmarshal(group.Members, &members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 3. 从成员列表中删除该用户
    for i, member := range members {
        if member == userId {
            // ★核心：使用append删除切片元素
            members = append(members[:i], members[i+1:]...)
            break
        }
    }

    // 4. JSON序列化：将切片转换回JSON
    if data, err := json.Marshal(members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    } else {
        group.Members = data
    }

    // 5. 更新群成员数量
    group.MemberCnt -= 1

    // 6. 保存群聊信息
    if res := dao.GormDB.Save(&group); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 7. 软删除会话记录
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true
    if res := dao.GormDB.Model(&model.Session{}).Where("send_id = ? AND receive_id = ?", userId, groupId).Update("deleted_at", deletedAt); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 8. 软删除联系人记录，并更新状态为"退群"
    if res := dao.GormDB.Model(&model.UserContact{}).Where("user_id = ? AND contact_id = ?", userId, groupId).Updates(map[string]interface{}{
        "deleted_at": deletedAt,
        "status":     contact_status_enum.QUIT_GROUP, // 退群状态
    }); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 9. 软删除申请记录
    if res := dao.GormDB.Model(&model.ContactApply{}).Where("contact_id = ? AND user_id = ?", groupId, userId).Update("deleted_at", deletedAt); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 10. ★清除Redis缓存（带错误处理）
	if err := myredis.DelKeysWithPattern("group_session_list_" + userId); err != nil {
		zlog.Error(err.Error())
	}
	if err := myredis.DelKeysWithPattern("my_joined_group_list_ " + userId); err != nil {
		zlog.Error(err.Error())
	}

    return "退群成功", 0
}
```

---

### 2.6 数据库操作说明

| 操作 | 表 | 条件 | 说明 |
|------|-----|------|------|
| SELECT | GroupInfo | uuid = groupId | 查询群聊信息 |
| UPDATE | GroupInfo | - | 更新Members和MemberCnt |
| UPDATE | Session | send_id = userId AND receive_id = groupId | 软删除会话 |
| UPDATE | UserContact | user_id = userId AND contact_id = groupId | 软删除联系人，状态改为退群 |
| UPDATE | ContactApply | contact_id = groupId AND user_id = userId | 软删除申请记录 |

---

### 2.7 关键概念详解

#### Slice删除元素

Go语言中没有直接删除切片元素的方法，需要使用`append`技巧实现：

```go
// 删除索引为i的元素
members = append(members[:i], members[i+1:]...)
```

**原理图解：**

假设原始切片 `members = ["A", "B", "C", "D"]`，要删除索引2的元素"C"：

```
原始切片：
索引:    0      1      2      3
值:   ["A",   "B",   "C",   "D"]

members[:2] = ["A", "B"]     // 前半部分（不含索引2）
members[3:] = ["D"]          // 后半部分（索引2+1之后）

append(members[:2], members[3:]...)
= append(["A", "B"], ["D"]...)
= ["A", "B", "D"]
```

**注意事项：**
1. 需要先找到要删除元素的索引
2. 使用`break`防止删除后继续遍历（切片长度已改变）
3. 如果删除的是最后一个元素，`members[i+1:]`为空切片，append仍能正常工作

#### 软删除（Soft Delete）

GORM的软删除是通过`DeletedAt`字段实现的：

```go
type Model struct {
    DeletedAt gorm.DeletedAt `gorm:"column:deleted_at;index"`
}

// 手动设置软删除
var deletedAt gorm.DeletedAt
deletedAt.Time = time.Now()
deletedAt.Valid = true

// 执行更新
db.Model(&model).Where(...).Update("deleted_at", deletedAt)
```

软删除后，记录仍然存在于数据库中，但查询时会被自动过滤。

---

## 三、POST /group/dismissGroup - 解散群聊

### 3.1 接口概述

群主解散群聊时调用此接口。该操作会软删除多张表中的相关记录：
- GroupInfo：群聊信息
- Session：所有成员与群聊的会话
- UserContact：所有成员与群聊的联系人关系
- ContactApply：所有与群聊相关的申请记录

**关键技术点：** 批量软删除多表

---

### 3.2 Request结构体定义

```go
// DismissGroupRequest 解散群聊请求
type DismissGroupRequest struct {
    OwnerId string `json:"owner_id"` // 群主ID
    GroupId string `json:"group_id"` // 群聊ID
}
```

**字段说明：**
- `GroupId`：要解散的群聊唯一标识UUID

---

### 3.3 Respond结构体定义

此接口无特殊返回数据，仅返回操作结果。

```go
// 标准响应
{
    "code": 0,              // 0表示成功，-1表示系统错误
    "message": "解散群聊成功" // 或错误信息
}
```

---

### 3.4 Controller层完整代码

```go
// api/v1/group_info_controller.go
// DismissGroup 解散群聊
func DismissGroup(c *gin.Context) {
    var req request.DismissGroupRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.DismissGroup(req.OwnerId, req.GroupId)
    JsonBack(c, message, ret, nil)
}
```

---

### 3.5 Service层完整代码

```go
// internal/service/gorm/group_info_service.go
// DismissGroup 解散群聊
func (g *groupInfoService) DismissGroup(ownerId, groupId string) (string, int) {
    // 初始化软删除时间戳
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true

    // 1. 软删除群聊信息
    if res := dao.GormDB.Model(&model.GroupInfo{}).Where("uuid = ?", groupId).Updates(
        map[string]interface{}{
            "deleted_at": deletedAt,
            "updated_at": deletedAt.Time,
        }); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 2. 软删除所有相关会话记录
    var sessionList []model.Session
    if res := dao.GormDB.Model(&model.Session{}).Where("receive_id = ?", groupId).Find(&sessionList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for _, session := range sessionList {
        if res := dao.GormDB.Model(&session).Updates(
            map[string]interface{}{
                "deleted_at": deletedAt,
            }); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }

    // 3. 软删除所有相关联系人记录
    var userContactList []model.UserContact
    if res := dao.GormDB.Model(&model.UserContact{}).Where("contact_id = ?", groupId).Find(&userContactList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    for _, userContact := range userContactList {
        if res := dao.GormDB.Model(&userContact).Update("deleted_at", deletedAt); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }

    // 4. 软删除所有相关申请记录
	var contactApplys []model.ContactApply
	if res := dao.GormDB.Model(&contactApplys).Where("contact_id = ?", groupId).Find(&contactApplys); res.Error != nil {
		if res.Error != gorm.ErrRecordNotFound {
			zlog.Info(res.Error.Error())
			return "无响应的申请记录需要删除", 0
		}
		zlog.Error(res.Error.Error())
		return constants.SYSTEM_ERROR, -1
	}
	for _, contactApply := range contactApplys {
		if res := dao.GormDB.Model(&contactApply).Update("deleted_at", deletedAt); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
	}

    // 5. ★清除Redis缓存（带错误处理）
	if err := myredis.DelKeysWithPattern("contact_mygroup_list_" + ownerId); err != nil {
		zlog.Error(err.Error())
	}
	if err := myredis.DelKeysWithPattern("group_session_list_" + ownerId); err != nil {
		zlog.Error(err.Error())
	}
	if err := myredis.DelKeysWithPrefix("my_joined_group_list"); err != nil {
		zlog.Error(err.Error())
	}

    return "解散群聊成功", 0
}
```

---

### 3.6 数据库操作说明

| 操作 | 表 | 条件 | 说明 |
|------|-----|------|------|
| UPDATE | GroupInfo | uuid = groupId | 软删除群聊 |
| SELECT + UPDATE | Session | receive_id = groupId | 查询并软删除所有相关会话 |
| SELECT + UPDATE | UserContact | contact_id = groupId | 查询并软删除所有相关联系人 |
| SELECT + UPDATE | ContactApply | contact_id = groupId | 查询并软删除所有相关申请 |

**批量删除流程图：**

```
解散群聊
    │
    ├── 软删除 GroupInfo (群聊本身)
    │
    ├── 软删除 Session (所有成员的会话)
    │       └── receive_id = groupId
    │
    ├── 软删除 UserContact (所有成员的联系人关系)
    │       └── contact_id = groupId
    │
    └── 软删除 ContactApply (所有相关申请)
            └── contact_id = groupId
```

---

### 3.7 关键概念详解

#### 批量软删除多表

解散群聊需要处理多张表的数据，是一个典型的多表操作场景。

**设计考虑：**

1. **事务处理**：理想情况下应该使用事务确保原子性，当前实现为简化版
2. **批量操作**：当前实现逐条更新，大数据量时可优化为批量UPDATE
3. **错误处理**：每步操作都检查错误，任一步骤失败立即返回

**优化建议（使用事务）：**

```go
// 使用事务确保原子性
err := dao.GormDB.Transaction(func(tx *gorm.DB) error {
    // 1. 软删除群聊
    if err := tx.Model(&model.GroupInfo{}).Where("uuid = ?", groupId).Update("deleted_at", deletedAt).Error; err != nil {
        return err
    }

    // 2. 软删除会话
    if err := tx.Model(&model.Session{}).Where("receive_id = ?", groupId).Update("deleted_at", deletedAt).Error; err != nil {
        return err
    }

    // 3. 软删除联系人
    if err := tx.Model(&model.UserContact{}).Where("contact_id = ?", groupId).Update("deleted_at", deletedAt).Error; err != nil {
        return err
    }

    // 4. 软删除申请
    if err := tx.Model(&model.ContactApply{}).Where("contact_id = ?", groupId).Update("deleted_at", deletedAt).Error; err != nil {
        return err
    }

    return nil
})
```

#### gorm.DeletedAt类型

```go
type DeletedAt sql.NullTime

// 设置值
var deletedAt gorm.DeletedAt
deletedAt.Time = time.Now()  // 删除时间
deletedAt.Valid = true        // 有效标记
```

---

## 四、总结对比

| 接口 | 主要操作 | 涉及表 | 关键技术 |
|------|----------|--------|----------|
| enterGroupDirectly | 添加成员 | GroupInfo, UserContact | JSON反序列化、append、JSON序列化 |
| leaveGroup | 移除成员 | GroupInfo, Session, UserContact, ContactApply | slice删除元素、软删除 |
| dismissGroup | 解散群聊 | GroupInfo, Session, UserContact, ContactApply | 批量软删除多表 |

---

## 五、相关数据模型

### GroupInfo（群聊信息表）

```go
type GroupInfo struct {
    Id        int64           `gorm:"column:id;primaryKey"`
    Uuid      string          `gorm:"column:uuid;uniqueIndex;type:char(20)"`
    Name      string          `gorm:"column:name;type:varchar(20)"`
    Notice    string          `gorm:"column:notice;type:varchar(500)"`
    Members   json.RawMessage `gorm:"column:members;type:json"`      // JSON存储成员列表
    MemberCnt int             `gorm:"column:member_cnt;default:1"`
    OwnerId   string          `gorm:"column:owner_id;type:char(20)"`
    AddMode   int8            `gorm:"column:add_mode;default:0"`    // 0.直接，1.审核
    Avatar    string          `gorm:"column:avatar;type:char(255)"`
    Status    int8            `gorm:"column:status;default:0"`     // 0.正常，1.禁用，2.解散
    CreatedAt time.Time       `gorm:"column:created_at;type:datetime"`
    DeletedAt gorm.DeletedAt  `gorm:"column:deleted_at;index"`
}
```

### UserContact（用户联系人表）

```go
type UserContact struct {
    Id          int64          `gorm:"column:id;primaryKey"`
    UserId      string         `gorm:"column:user_id;index;type:char(20)"`
    ContactId   string         `gorm:"column:contact_id;index;type:char(20)"`
    ContactType int8           `gorm:"column:contact_type"`  // 0.用户，1.群聊
    Status      int8           `gorm:"column:status"`        // 0.正常，1.拉黑...
    CreatedAt   time.Time      `gorm:"column:created_at;type:datetime"`
    DeletedAt   gorm.DeletedAt `gorm:"column:deleted_at;type:datetime;index"`
}
```

### Session（会话表）

```go
type Session struct {
    Id          int64          `gorm:"column:id;primaryKey"`
    Uuid        string         `gorm:"column:uuid;uniqueIndex;type:char(20)"`
    SendId      string         `gorm:"column:send_id;index;type:char(20)"`
    ReceiveId   string         `gorm:"column:receive_id;index;type:char(20)"`
    ReceiveName string         `gorm:"column:receive_name;type:varchar(20)"`
    Avatar      string         `gorm:"column:avatar;type:char(255)"`
    CreatedAt   time.Time      `gorm:"column:created_at;index;type:datetime"`
    DeletedAt   gorm.DeletedAt `gorm:"column:deleted_at;index;type:datetime"`
}
```

### ContactApply（联系人申请表）

```go
type ContactApply struct {
    Id          int64          `gorm:"column:id;primaryKey"`
    Uuid        string         `gorm:"column:uuid;uniqueIndex;type:char(20)"`
    UserId      string         `gorm:"column:user_id;index;type:char(20)"`
    ContactId   string         `gorm:"column:contact_id;index;type:char(20)"`
    ContactType int8           `gorm:"column:contact_type"`  // 0.用户，1.群聊
    Status      int8           `gorm:"column:status"`         // 0.申请中，1.通过，2.拒绝
    Message     string         `gorm:"column:message;type:varchar(100)"`
    LastApplyAt time.Time      `gorm:"column:last_apply_at;type:datetime"`
    DeletedAt   gorm.DeletedAt `gorm:"column:deleted_at;index;type:datetime"`
}
```