# 教学文档 14: 会话管理

## 本章完成的接口

| 接口 | 路由 | 功能描述 |
|------|------|---------|
| `OpenSession` | `/session/openSession` | 打开/创建会话 |
| `GetUserSessionList` | `/session/getUserSessionList` | 获取用户会话列表 |
| `GetGroupSessionList` | `/session/getGroupSessionList` | 获取群聊会话列表 |
| `DeleteSession` | `/session/deleteSession` | 删除会话 |
| `CheckOpenSessionAllowed` | `/session/checkOpenSessionAllowed` | 检查是否允许发起会话 |

### 开发顺序（重要）

```
Model → DAO → Service → Controller → 路由注册

① Model层: 会话模型定义（session.go）
② DAO层: 数据库连接已在前序文档完成
③ Service层: 会话业务逻辑实现（session_service.go）
④ Controller层: HTTP请求处理（session_controller.go）
⑤ 路由注册: 在https_server中注册路由
```

---

## Redis方法引用

本文档使用的Redis方法已在 **文档07** 完整定义：

| 方法 | 作用 |
|------|------|
| `GetKeyNilIsErr` | 获取key，不存在返回redis.Nil错误 |
| `GetKeyWithPrefixNilIsErr` | 前缀查找单个key（使用Scan安全） |
| `DelKeysWithPattern` | 删除带前缀的所有键（使用Scan安全） |

> Redis方法完整实现请参考 **文档07-Redis邮箱验证码详细步骤.md** 的"六、创建Redis服务"章节。

---

会话（Session）是聊天系统中用户与用户、用户与群聊之间进行消息交互的核心载体。本章将详细介绍五个核心会话管理接口的实现。

---

## 数据模型回顾

在实现会话管理接口之前，先回顾Session表的数据模型：

**文件位置:** `internal/model/session.go`

```go
package model

import (
    "time"
    "gorm.io/gorm"
)

type Session struct {
    Id            int64          `gorm:"column:id;primaryKey;comment:自增id"`
    Uuid          string         `gorm:"column:uuid;uniqueIndex;type:char(20);comment:会话uuid"`
    SendId        string         `gorm:"column:send_id;index;type:char(20);not null;comment:创建会话人id"`
    ReceiveId     string         `gorm:"column:receive_id;index;type:char(20);not null;comment:接受会话人id"`
    ReceiveName   string         `gorm:"column:receive_name;type:varchar(20);not null;comment:名称"`
    Avatar        string         `gorm:"column:avatar;type:char(255);default:default_avatar.png;not null;comment:头像"`
    CreatedAt     time.Time      `gorm:"column:created_at;index;type:datetime;comment:创建时间"`
    DeletedAt     gorm.DeletedAt `gorm:"column:deleted_at;index;type:datetime;comment:删除时间"`
}
```

**字段说明**：
- `Uuid`：会话唯一标识，格式为"S" + 11位随机字符
- `SendId`：发起会话的用户ID（类型为用户）
- `ReceiveId`：接收方的ID（可以是用户或群聊）
- `ReceiveName`：接收方的名称（用户昵称或群名）
- `Avatar`：接收方头像

---

## 一、POST /session/openSession - 打开会话

### 1. 接口概述

打开会话接口用于用户与联系人（用户或群聊）建立聊天会话。当用户点击某个联系人时，前端调用此接口。

**核心逻辑**：
1. 先检查Redis缓存是否存在会话
2. 如果缓存不存在，从数据库查询会话是否已存在
3. 如果数据库也不存在，调用CreateSession创建新会话
4. 如果已存在，直接返回现有会话的Uuid

### 2. Request结构体定义

**文件位置:** `internal/dto/request/open_session_request.go`

```go
package request

type OpenSessionRequest struct {
    SendId    string `json:"send_id"`
    ReceiveId string `json:"receive_id"`
}
```

**CreateSessionRequest结构体定义**

**文件位置:** `internal/dto/request/create_session_request.go`

```go
package request

type CreateSessionRequest struct {
    SendId    string `json:"send_id"`
    ReceiveId string `json:"receive_id"`
}
```

> 注意：OpenSessionRequest和CreateSessionRequest字段完全相同，但分开定义便于后续扩展不同字段。

### 3. Respond结构体定义

打开会话接口直接返回会话Uuid字符串，不使用复杂的响应结构体。

```go
// 成功响应示例
{
    "code": 200,
    "message": "会话创建成功",
    "data": "S12345678901"
}
```

### 4. Controller层完整代码

**文件位置:** `api/v1/session_controller.go`

```go
package v1

import (
    "github.com/gin-gonic/gin"
    "kama_chat_server/internal/dto/request"
    "kama_chat_server/internal/service/gorm"
    "kama_chat_server/pkg/constants"
    "kama_chat_server/pkg/zlog"
    "net/http"
)

// OpenSession 打开会话
func OpenSession(c *gin.Context) {
    var openSessionReq request.OpenSessionRequest
    if err := c.BindJSON(&openSessionReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, sessionId, ret := gorm.SessionService.OpenSession(openSessionReq)
    JsonBack(c, message, ret, sessionId)
}
```

### 5. Service层完整代码

**文件位置:** `internal/service/gorm/session_service.go`

```go
package gorm

import (
    "encoding/json"
    "errors"
    "fmt"
    "github.com/go-redis/redis/v8"
    "gorm.io/gorm"
    "kama_chat_server/internal/dao"
    "kama_chat_server/internal/dto/request"
    "kama_chat_server/internal/dto/respond"
    "kama_chat_server/internal/model"
    myredis "kama_chat_server/internal/service/redis"
    "kama_chat_server/pkg/constants"
    "kama_chat_server/pkg/enum/contact/contact_status_enum"
    "kama_chat_server/pkg/enum/group_info/group_status_enum"
    "kama_chat_server/pkg/enum/user_info/user_status_enum"
    "kama_chat_server/pkg/util/random"
    "kama_chat_server/pkg/zlog"
    "time"
)

type sessionService struct{}

var SessionService = new(sessionService)

// OpenSession 打开会话
func (s *sessionService) OpenSession(req request.OpenSessionRequest) (string, string, int) {
    rspString, err := myredis.GetKeyWithPrefixNilIsErr("session_" + req.SendId + "_" + req.ReceiveId)
    if err != nil {
        if errors.Is(err, redis.Nil) {
            var session model.Session
            if res := dao.GormDB.Where("send_id = ? and receive_id = ?", req.SendId, req.ReceiveId).First(&session); res.Error != nil {
                if errors.Is(res.Error, gorm.ErrRecordNotFound) {
                    zlog.Info("会话没有找到，将新建会话")
                    createReq := request.CreateSessionRequest{
                        SendId:    req.SendId,
                        ReceiveId: req.ReceiveId,
                    }
                    return s.CreateSession(createReq)
                }
            }
            //rspString, err := json.Marshal(session)
            //if err != nil {
            //	zlog.Error(err.Error())
            //}
            //if err := myredis.SetKeyEx("session_"+req.SendId+"_"+req.ReceiveId+"_"+session.Uuid, string(rspString), time.Minute*constants.REDIS_TIMEOUT); err != nil {
            //	zlog.Error(err.Error())
            //}
            return "会话创建成功", session.Uuid, 0
        } else {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, "", -1
        }
    }
    var session model.Session
    if err := json.Unmarshal([]byte(rspString), &session); err != nil {
        zlog.Error(err.Error())
    }
    return "会话创建成功", session.Uuid, 0
}

// CreateSession 创建会话
func (s *sessionService) CreateSession(req request.CreateSessionRequest) (string, string, int) {
    var user model.UserInfo
    if res := dao.GormDB.Where("uuid = ?", req.SendId).First(&user); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, "", -1
    }
    var session model.Session
    session.Uuid = fmt.Sprintf("S%s", random.GetNowAndLenRandomString(11))
    session.SendId = req.SendId
    session.ReceiveId = req.ReceiveId
    session.CreatedAt = time.Now()
    if req.ReceiveId[0] == 'U' {
        var receiveUser model.UserInfo
        if res := dao.GormDB.Where("uuid = ?", req.ReceiveId).First(&receiveUser); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, "", -1
        }
        if receiveUser.Status == user_status_enum.DISABLE {
            zlog.Error("该用户被禁用了")
            return "该用户被禁用了", "", -2
        } else {
            session.ReceiveName = receiveUser.Nickname
            session.Avatar = receiveUser.Avatar
        }
    } else {
        var receiveGroup model.GroupInfo
        if res := dao.GormDB.Where("uuid = ?", req.ReceiveId).First(&receiveGroup); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, "", -1
        }
        if receiveGroup.Status == group_status_enum.DISABLE {
            zlog.Error("该群聊被禁用了")
            return "该群聊被禁用了", "", -2
        } else {
            session.ReceiveName = receiveGroup.Name
            session.Avatar = receiveGroup.Avatar
        }
    }

    if res := dao.GormDB.Create(&session); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, "", -1
    }
    if err := myredis.DelKeysWithPattern("group_session_list_" + req.SendId); err != nil {
        zlog.Error(err.Error())
    }
    if err := myredis.DelKeysWithPattern("session_list_" + req.SendId); err != nil {
        zlog.Error(err.Error())
    }
    return "会话创建成功", session.Uuid, 0
}
```

### 6. 数据库操作说明

| 操作 | GORM方法 | SQL语句 |
|------|----------|---------|
| 查询会话 | `Where("send_id = ? and receive_id = ?", ...).First(&session)` | `SELECT * FROM session WHERE send_id = 'U...' AND receive_id = 'U...' AND deleted_at IS NULL LIMIT 1` |
| 查询用户信息 | `Where("uuid = ?", ...).First(&user)` | `SELECT * FROM user_info WHERE uuid = 'U...' AND deleted_at IS NULL LIMIT 1` |
| 查询群聊信息 | `Where("uuid = ?", ...).First(&group)` | `SELECT * FROM group_info WHERE uuid = 'G...' AND deleted_at IS NULL LIMIT 1` |
| 创建会话 | `Create(&session)` | `INSERT INTO session (uuid, send_id, receive_id, ...) VALUES (...)` |

### 7. 关键概念详解

#### 会话Uuid格式

```go
session.Uuid = fmt.Sprintf("S%s", random.GetNowAndLenRandomString(11))
```

**格式规则**：
- 首字符为"S"标识会话类型（Session）
- 后接11位随机字符，确保全局唯一性
- 总长度为12位字符

**对比其他Uuid格式**：
- 用户Uuid："U" + 11位随机字符
- 群聊Uuid："G" + 11位随机字符
- 消息Uuid："M" + 11位随机字符

#### 禁用状态检查

```go
// 创建会话时需要检查接收方是否被禁用
if receiveUser.Status == user_status_enum.DISABLE {
    return "该用户被禁用了", "", -2
}
if receiveGroup.Status == group_status_enum.DISABLE {
    return "该群聊被禁用了", "", -2
}
```

---

## 二、POST /session/getUserSessionList - 获取用户会话列表

### 1. 接口概述

获取用户会话列表接口用于查询当前用户的所有单聊会话。当用户点击会话列表中的"用户"分类时调用。

**核心逻辑**：
1. 先从Redis获取缓存
2. 如果缓存不存在，从数据库查询所有会话
3. 按创建时间倒序排列（created_at DESC）
4. 筛选出用户类型的会话（ReceiveId首字符为'U'）
5. 返回会话列表信息并写入Redis缓存

### 2. Request结构体定义

**文件位置:** `internal/dto/request/ownlist_request.go`

```go
package request

type OwnlistRequest struct {
    OwnerId string `json:"owner_id"`
}
```

### 3. Respond结构体定义

**文件位置:** `internal/dto/respond/user_sessionlist_respond.go`

```go
package respond

type UserSessionListRespond struct {
    SessionId string `json:"session_id"`
    Avatar    string `json:"avatar"`
    UserId    string `json:"user_id"`
    Username  string `json:"user_name"`
}
```

### 4. Controller层完整代码

**文件位置:** `api/v1/session_controller.go`

```go
// GetUserSessionList 获取用户会话列表
func GetUserSessionList(c *gin.Context) {
    var getUserSessionListReq request.OwnlistRequest
    if err := c.BindJSON(&getUserSessionListReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, sessionList, ret := gorm.SessionService.GetUserSessionList(getUserSessionListReq.OwnerId)
    JsonBack(c, message, ret, sessionList)
}
```

### 5. Service层完整代码

**文件位置:** `internal/service/gorm/session_service.go`

```go
// GetUserSessionList 获取用户会话列表
func (s *sessionService) GetUserSessionList(ownerId string) (string, []respond.UserSessionListRespond, int) {
    rspString, err := myredis.GetKeyNilIsErr("session_list_" + ownerId)
    if err != nil {
        if errors.Is(err, redis.Nil) {
            var sessionList []model.Session
            if res := dao.GormDB.Order("created_at DESC").Where("send_id = ?", ownerId).Find(&sessionList); res.Error != nil {
                if errors.Is(res.Error, gorm.ErrRecordNotFound) {
                    zlog.Info("未创建用户会话")
                    return "未创建用户会话", nil, 0
                } else {
                    zlog.Error(res.Error.Error())
                    return constants.SYSTEM_ERROR, nil, -1
                }
            }
            var sessionListRsp []respond.UserSessionListRespond
            for i := 0; i < len(sessionList); i++ {
                if sessionList[i].ReceiveId[0] == 'U' {
                    sessionListRsp = append(sessionListRsp, respond.UserSessionListRespond{
                        SessionId: sessionList[i].Uuid,
                        Avatar:    sessionList[i].Avatar,
                        UserId:    sessionList[i].ReceiveId,
                        Username:  sessionList[i].ReceiveName,
                    })
                }
            }
            rspString, err := json.Marshal(sessionListRsp)
            if err != nil {
                zlog.Error(err.Error())
            }
            if err := myredis.SetKeyEx("session_list_"+ownerId, string(rspString), time.Minute*constants.REDIS_TIMEOUT); err != nil {
                zlog.Error(err.Error())
            }
            return "获取成功", sessionListRsp, 0
        } else {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }
    var rsp []respond.UserSessionListRespond
    if err := json.Unmarshal([]byte(rspString), &rsp); err != nil {
        zlog.Error(err.Error())
    }
    return "获取成功", rsp, 0
}
```

### 6. 数据库操作说明

| 操作 | GORM方法 | SQL语句 |
|------|----------|---------|
| 查询会话列表 | `Order("created_at DESC").Where("send_id = ?", ...).Find(&sessionList)` | `SELECT * FROM session WHERE send_id = 'U...' AND deleted_at IS NULL ORDER BY created_at DESC` |

**排序说明**：
- `Order("created_at DESC")`确保最新创建的会话排在前面
- DESC表示降序（Descending），时间越新排在越前面

---

## 三、POST /session/getGroupSessionList - 获取群聊会话列表

### 1. 接口概述

获取群聊会话列表接口用于查询当前用户的所有群聊会话。当用户点击会话列表中的"群聊"分类时调用。

### 2. Request结构体定义

与getUserSessionList共用 `OwnlistRequest`。

### 3. Respond结构体定义

**文件位置:** `internal/dto/respond/group_sessionlist_respond.go`

```go
package respond

type GroupSessionListRespond struct {
    SessionId string `json:"session_id"`
    Avatar    string `json:"avatar"`
    GroupId   string `json:"group_id"`
    GroupName string `json:"group_name"`
}
```

### 4. Controller层完整代码

**文件位置:** `api/v1/session_controller.go`

```go
// GetGroupSessionList 获取群聊会话列表
func GetGroupSessionList(c *gin.Context) {
    var getGroupListReq request.OwnlistRequest
    if err := c.BindJSON(&getGroupListReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, groupList, ret := gorm.SessionService.GetGroupSessionList(getGroupListReq.OwnerId)
    JsonBack(c, message, ret, groupList)
}
```

### 5. Service层完整代码

**文件位置:** `internal/service/gorm/session_service.go`

```go
// GetGroupSessionList 获取群聊会话列表
func (s *sessionService) GetGroupSessionList(ownerId string) (string, []respond.GroupSessionListRespond, int) {
    rspString, err := myredis.GetKeyNilIsErr("group_session_list_" + ownerId)
    if err != nil {
        if errors.Is(err, redis.Nil) {
            var sessionList []model.Session
            if res := dao.GormDB.Order("created_at DESC").Where("send_id = ?", ownerId).Find(&sessionList); res.Error != nil {
                if errors.Is(res.Error, gorm.ErrRecordNotFound) {
                    zlog.Info("未创建群聊会话")
                    return "未创建群聊会话", nil, 0
                } else {
                    zlog.Error(res.Error.Error())
                    return constants.SYSTEM_ERROR, nil, -1
                }
            }
            var sessionListRsp []respond.GroupSessionListRespond
            for i := 0; i < len(sessionList); i++ {
                if sessionList[i].ReceiveId[0] == 'G' {
                    sessionListRsp = append(sessionListRsp, respond.GroupSessionListRespond{
                        SessionId: sessionList[i].Uuid,
                        Avatar:    sessionList[i].Avatar,
                        GroupId:   sessionList[i].ReceiveId,
                        GroupName: sessionList[i].ReceiveName,
                    })
                }
            }
            rspString, err := json.Marshal(sessionListRsp)
            if err != nil {
                zlog.Error(err.Error())
            }
            if err := myredis.SetKeyEx("group_session_list_"+ownerId, string(rspString), time.Minute*constants.REDIS_TIMEOUT); err != nil {
                zlog.Error(err.Error())
            }
            return "获取成功", sessionListRsp, 0
        } else {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }
    var rsp []respond.GroupSessionListRespond
    if err := json.Unmarshal([]byte(rspString), &rsp); err != nil {
        zlog.Error(err.Error())
    }
    return "获取成功", rsp, 0
}
```

---

## 四、POST /session/deleteSession - 删除会话

### 1. 接口概述

删除会话接口用于用户删除某个聊天会话记录。

**核心逻辑**：
1. 根据SessionId查询会话记录
2. 使用软删除方式删除（设置DeletedAt字段）
3. 清理Redis缓存

### 2. Request结构体定义

**文件位置:** `internal/dto/request/delete_session_request.go`

```go
package request

type DeleteSessionRequest struct {
    OwnerId   string `json:"owner_id"`
    SessionId string `json:"session_id"`
}
```

### 3. Respond结构体定义

删除会话接口成功时只返回成功消息，不返回数据。

```go
// 成功响应示例
{
    "code": 200,
    "message": "删除成功"
}
```

### 4. Controller层完整代码

**文件位置:** `api/v1/session_controller.go`

```go
// DeleteSession 删除会话
func DeleteSession(c *gin.Context) {
    var deleteSessionReq request.DeleteSessionRequest
    if err := c.BindJSON(&deleteSessionReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.SessionService.DeleteSession(deleteSessionReq.OwnerId, deleteSessionReq.SessionId)
    JsonBack(c, message, ret, nil)
}
```

### 5. Service层完整代码

**文件位置:** `internal/service/gorm/session_service.go`

```go
// DeleteSession 删除会话
func (s *sessionService) DeleteSession(ownerId, sessionId string) (string, int) {

    var session model.Session
    if res := dao.GormDB.Where("uuid = ?", sessionId).Find(&session); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    session.DeletedAt.Valid = true
    session.DeletedAt.Time = time.Now()
    if res := dao.GormDB.Save(&session); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    //if err := myredis.DelKeysWithSuffix(sessionId); err != nil {
    //	zlog.Error(err.Error())
    //}
    if err := myredis.DelKeysWithPattern("group_session_list_" + ownerId); err != nil {
        zlog.Error(err.Error())
    }
    if err := myredis.DelKeysWithPattern("session_list_" + ownerId); err != nil {
        zlog.Error(err.Error())
    }
    return "删除成功", 0
}
```

### 6. 数据库操作说明

| 操作 | GORM方法 | SQL语句 |
|------|----------|---------|
| 查询会话 | `Where("uuid = ?", ...).Find(&session)` | `SELECT * FROM session WHERE uuid = 'S...' AND deleted_at IS NULL` |
| 软删除保存 | `Save(&session)` | `UPDATE session SET deleted_at = '...' WHERE id = ...` |

---

## 五、POST /session/checkOpenSessionAllowed - 检查是否允许发起会话

### 1. 接口概述

检查是否允许发起会话接口用于在用户点击联系人时，验证是否可以打开会话。

**核心逻辑**：
1. 查询联系人关系（UserContact）
2. 检查是否被拉黑或拉黑对方
3. 检查接收方是否被禁用

### 2. Request结构体定义

**文件位置:** `internal/dto/request/create_session_request.go`

```go
package request

type CreateSessionRequest struct {
    SendId    string `json:"send_id"`
    ReceiveId string `json:"receive_id"`
}
```

### 3. Respond结构体定义

返回布尔值，表示是否允许发起会话。

```go
// 允许发起会话响应示例
{
    "code": 200,
    "message": "可以发起会话",
    "data": true
}
```

### 4. Controller层完整代码

**文件位置:** `api/v1/session_controller.go`

```go
// CheckOpenSessionAllowed 检查是否可以打开会话
func CheckOpenSessionAllowed(c *gin.Context) {
    var req request.CreateSessionRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, res, ret := gorm.SessionService.CheckOpenSessionAllowed(req.SendId, req.ReceiveId)
    JsonBack(c, message, ret, res)
}
```

### 5. Service层完整代码

**文件位置:** `internal/service/gorm/session_service.go`

```go
// CheckOpenSessionAllowed 检查是否允许发起会话
func (s *sessionService) CheckOpenSessionAllowed(sendId, receiveId string) (string, bool, int) {
    var contact model.UserContact
    if res := dao.GormDB.Where("user_id = ? and contact_id = ?", sendId, receiveId).First(&contact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, false, -1
    }
    if contact.Status == contact_status_enum.BE_BLACK {
        return "已被对方拉黑，无法发起会话", false, -2
    } else if contact.Status == contact_status_enum.BLACK {
        return "已拉黑对方，先解除拉黑状态才能发起会话", false, -2
    }
    if receiveId[0] == 'U' {
        var user model.UserInfo
        if res := dao.GormDB.Where("uuid = ?", receiveId).First(&user); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, false, -1
        }
        if user.Status == user_status_enum.DISABLE {
            zlog.Info("对方已被禁用，无法发起会话")
            return "对方已被禁用，无法发起会话", false, -2
        }
    } else {
        var group model.GroupInfo
        if res := dao.GormDB.Where("uuid = ?", receiveId).First(&group); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, false, -1
        }
        if group.Status == group_status_enum.DISABLE {
            zlog.Info("对方已被禁用，无法发起会话")
            return "对方已被禁用，无法发起会话", false, -2
        }
    }
    return "可以发起会话", true, 0
}
```

---

## 路由配置

在HTTP服务器中注册会话管理路由：

```go
// internal/https_server/https_server.go
GE.POST("/session/openSession", v1.OpenSession)
GE.POST("/session/getUserSessionList", v1.GetUserSessionList)
GE.POST("/session/getGroupSessionList", v1.GetGroupSessionList)
GE.POST("/session/deleteSession", v1.DeleteSession)
GE.POST("/session/checkOpenSessionAllowed", v1.CheckOpenSessionAllowed)
```

---

## 总结

| 接口 | 核心功能 | 关键技术点 |
|------|----------|------------|
| openSession | 打开/创建会话 | Redis缓存、gorm.ErrRecordNotFound判断、禁用状态检查 |
| getUserSessionList | 获取用户会话列表 | Redis缓存、Order排序、ReceiveId[0]类型判断 |
| getGroupSessionList | 获取群聊会话列表 | Redis缓存、筛选群聊类型会话 |
| deleteSession | 删除会话 | 软删除机制、Redis缓存清理 |
| checkOpenSessionAllowed | 检查是否允许发起会话 | 拉黑状态检查、禁用状态检查 |

**开发要点**：
1. 打开会话时必须先检查是否已存在，避免重复创建
2. 创建会话时检查接收方是否被禁用
3. 会话列表按时间倒序排列，用户体验更好
4. 使用软删除保留数据，便于后续数据恢复或审计
5. 通过Uuid首字符区分类型（U-用户，G-群聊，S-会话）