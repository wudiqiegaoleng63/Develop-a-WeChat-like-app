# 教学文档 14: 管理员群组接口

## 本章完成的接口

| 接口 | 路由 | 功能描述 |
|------|------|---------|
| `GetGroupInfoList` | `/group/getGroupInfoList` | 获取群组列表（管理员） |
| `DeleteGroups` | `/group/deleteGroups` | 批量删除群组（管理员） |
| `SetGroupsStatus` | `/group/setGroupsStatus` | 设置群组状态（管理员） |

### 开发顺序（重要）

```
Model → DAO → Service → Controller → 路由注册

① Model层: 群组模型已定义（group_info.go）
② DAO层: 数据库连接已在前序文档完成
③ Service层: 管理员群组业务逻辑实现（group_info_service.go）
④ Controller层: HTTP请求处理（group_info_controller.go）
⑤ 路由注册: 在https_server中注册路由
```

本文档详细介绍管理员专用的三个群组管理接口：获取群组列表、批量删除群组、设置群组状态。

---

## 一、POST /group/getGroupInfoList - 获取群组列表（管理员）

### 1.1 接口概述

**功能描述**：管理员获取所有群组列表，包括已软删除的群组记录。

**核心特点**：
- 使用`Unscoped()`查询，包含软删除记录
- 不使用Redis缓存（管理员数量少，缓存管理复杂）
- 返回群组基本信息和删除状态

---

### 1.2 Request结构体定义

此接口无需请求参数，管理员身份通过中间件验证。

---

### 1.3 Respond结构体定义

```go
// internal/dto/respond/get_group_list_respond.go
package respond

// GetGroupListRespond 获取群组列表响应结构体
type GetGroupListRespond struct {
    Uuid      string `json:"uuid"`       // 群组UUID
    Name      string `json:"name"`       // 群组名称
    OwnerId   string `json:"owner_id"`   // 群主UUID
    Status    int8   `json:"status"`     // 状态：0=正常，1=禁用，2=解散
    IsDeleted bool   `json:"is_deleted"` // 是否已软删除
}
```

**成功响应示例**：
```json
{
    "code": 0,
    "message": "获取成功",
    "data": [
        {
            "uuid": "G20260420123",
            "name": "技术交流群",
            "owner_id": "U12345678901",
            "status": 0,
            "is_deleted": false
        },
        {
            "uuid": "G20260420124",
            "name": "已解散的群",
            "owner_id": "U12345678902",
            "status": 2,
            "is_deleted": true
        }
    ]
}
```

---

### 1.4 Controller层完整代码

```go
// api/v1/group_info_controller.go

// GetGroupInfoList 获取群聊列表 - 管理员
func GetGroupInfoList(c *gin.Context) {
    message, groupList, ret := gorm.GroupInfoService.GetGroupInfoList()
    JsonBack(c, message, ret, groupList)
}
```

**代码解析**：
- 无请求参数，直接调用Service层
- 管理员身份通过中间件验证（如有配置）
- 返回群组列表数据

---

### 1.5 Service层完整代码

```go
// internal/service/gorm/group_info_service.go

// GetGroupInfoList 获取群聊列表 - 管理员
// 管理员少，而且如果用户更改了，那么管理员会一直频繁删除redis，更新redis，比较麻烦，所以管理员暂时不使用redis缓存
func (g *groupInfoService) GetGroupInfoList() (string, []respond.GetGroupListRespond, int) {
    var groupList []model.GroupInfo
    // ★Unscoped()：查询包含软删除记录
    if res := dao.GormDB.Unscoped().Find(&groupList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    var rsp []respond.GetGroupListRespond
    for _, group := range groupList {
        rp := respond.GetGroupListRespond{
            Uuid:    group.Uuid,
            Name:    group.Name,
            OwnerId: group.OwnerId,
            Status:  group.Status,
        }
        // ★判断是否已软删除
        if group.DeletedAt.Valid {
            rp.IsDeleted = true
        } else {
            rp.IsDeleted = false
        }
        rsp = append(rsp, rp)
    }
    return "获取成功", rsp, 0
}
```

**关键概念说明**：
- **Unscoped()**：GORM默认查询会过滤软删除记录，使用`Unscoped()`可查询所有记录
- **DeletedAt.Valid**：判断是否已软删除，Valid=true表示已删除

---

### 1.6 数据库操作说明

| 操作 | 表 | 条件 | 说明 |
|------|-----|------|------|
| Find | GroupInfo | 无条件 | 查询所有群组（含软删除） |

---

## 二、POST /group/deleteGroups - 批量删除群组（管理员）

### 2.1 接口概述

**功能描述**：管理员批量删除群组，执行与解散群组类似的操作，支持批量处理。

**核心特点**：
- 批量操作：支持同时删除多个群组
- 多表删除：同步删除Session、UserContact、ContactApply记录
- 软删除机制：使用deleted_at字段标记

---

### 2.2 Request结构体定义

```go
// internal/dto/request/delete_groups_request.go
package request

// DeleteGroupsRequest 批量删除群组请求
type DeleteGroupsRequest struct {
    UuidList []string `json:"uuid_list"` // 待删除群组UUID列表
}
```

**字段说明**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| uuid_list | []string | 是 | 要删除的群组UUID数组，支持批量操作 |

---

### 2.3 Respond结构体定义

```go
// 本接口无特殊返回数据，使用通用响应格式
// 成功响应：
{
    "code": 0,
    "message": "解散/删除群聊成功",
    "data": null
}
```

---

### 2.4 Controller层完整代码

```go
// api/v1/group_info_controller.go

// DeleteGroups 删除列表中群聊 - 管理员
func DeleteGroups(c *gin.Context) {
    var req request.DeleteGroupsRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.DeleteGroups(req.UuidList)
    JsonBack(c, message, ret, nil)
}
```

---

### 2.5 Service层完整代码

```go
// internal/service/gorm/group_info_service.go

// DeleteGroups 删除列表中群聊 - 管理员
func (g *groupInfoService) DeleteGroups(uuidList []string) (string, int) {
    for _, uuid := range uuidList {
        var deletedAt gorm.DeletedAt
        deletedAt.Time = time.Now()
        deletedAt.Valid = true
        
        // 1. 软删除群聊
        if res := dao.GormDB.Model(&model.GroupInfo{}).Where("uuid = ?", uuid).Update("deleted_at", deletedAt); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        
        // 2. 删除会话
        var sessionList []model.Session
        if res := dao.GormDB.Model(&model.Session{}).Where("receive_id = ?", uuid).Find(&sessionList); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        for _, session := range sessionList {
            if res := dao.GormDB.Model(&session).Update("deleted_at", deletedAt); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
        
        // 3. 删除联系人
        var userContactList []model.UserContact
        if res := dao.GormDB.Model(&model.UserContact{}).Where("contact_id = ?", uuid).Find(&userContactList); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        
        for _, userContact := range userContactList {
            if res := dao.GormDB.Model(&userContact).Update("deleted_at", deletedAt); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
        
        // 4. 删除申请记录
        var contactApplys []model.ContactApply
        if res := dao.GormDB.Model(&contactApplys).Where("contact_id = ?", uuid).Find(&contactApplys); res.Error != nil {
            if res.Error != gorm.ErrRecordNotFound {
                zlog.Info(res.Error.Error())
                return "无响应的申请记录需要删除", 0
            }
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        for _, contactApply := range contactApplys {
            if res := dao.GormDB.Model(&contactApply).Update("deleted_at", deletedAt); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
    }
    
    // ★5. 清理Redis缓存（带错误处理）
    if err := myredis.DelKeysWithPrefix("contact_mygroup_list"); err != nil {
        zlog.Error(err.Error())
    }
    if err := myredis.DelKeysWithPrefix("group_session_list"); err != nil {
        zlog.Error(err.Error())
    }
    if err := myredis.DelKeysWithPrefix("group_session_list"); err != nil {
        zlog.Error(err.Error())
    }
    return "解散/删除群聊成功", 0
}
```

---

### 2.6 数据库操作说明

**批量删除流程**：
```
遍历uuidList中的每个群组UUID
    │
    ├── 软删除 GroupInfo (群聊本身)
    │       └── uuid = ?
    │
    ├── 软删除 Session (所有成员的会话)
    │       └── receive_id = ?
    │
    ├── 软删除 UserContact (所有成员的联系人关系)
    │       └── contact_id = ?
    │
    └── 软删除 ContactApply (所有相关申请)
            └── contact_id = ?
    
清理Redis缓存
```

---

## 三、POST /group/setGroupsStatus - 设置群组状态（管理员）

### 3.1 接口概述

**功能描述**：管理员批量设置群组的启用/禁用状态。

**核心特点**：
- 批量操作：支持同时设置多个群组状态
- 禁用时同步删除会话：确保禁用群组不在用户会话列表显示
- 状态枚举：0=正常，1=禁用，2=解散

---

### 3.2 Request结构体定义

```go
// internal/dto/request/set_groups_status_request.go
package request

// SetGroupsStatusRequest 设置群组状态请求
type SetGroupsStatusRequest struct {
    UuidList []string `json:"uuid_list"` // 群组UUID列表
    Status   int8     `json:"status"`    // 目标状态：0=正常，1=禁用
}
```

**字段说明**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| uuid_list | []string | 是 | 要设置的群组UUID数组 |
| status | int8 | 是 | 目标状态值 |

---

### 3.3 Respond结构体定义

```go
// 本接口无特殊返回数据，使用通用响应格式
// 成功响应：
{
    "code": 0,
    "message": "设置成功",
    "data": null
}
```

---

### 3.4 Controller层完整代码

```go
// api/v1/group_info_controller.go

// SetGroupsStatus 设置群聊是否启用
func SetGroupsStatus(c *gin.Context) {
    var req request.SetGroupsStatusRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.SetGroupsStatus(req.UuidList, req.Status)
    JsonBack(c, message, ret, nil)
}
```

---

### 3.5 Service层完整代码

```go
// internal/service/gorm/group_info_service.go

// SetGroupsStatus 设置群聊是否启用
func (g *groupInfoService) SetGroupsStatus(uuidList []string, status int8) (string, int) {
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true
    
    for _, uuid := range uuidList {
        // 1. 更新群组状态
        if res := dao.GormDB.Model(&model.GroupInfo{}).Where("uuid = ?", uuid).Update("status", status); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        
        // ★2. 如果是禁用状态，同步删除会话
        if status == group_status_enum.DISABLE {
            var sessionList []model.Session
            if res := dao.GormDB.Model(&sessionList).Where("receive_id = ?", uuid).Find(&sessionList); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
            for _, session := range sessionList {
                if res := dao.GormDB.Model(&session).Update("deleted_at", deletedAt); res.Error != nil {
                    zlog.Error(res.Error.Error())
                    return constants.SYSTEM_ERROR, -1
                }
            }
        }
    }
    
    // ★3. Redis缓存清理（当前代码被注释，暂不启用）
    //for _, uuid := range uuidList {
    //	if err := myredis.DelKeysWithPattern("group_info_" + uuid); err != nil {
    //		zlog.Error(err.Error())
    //	}
    //}
    
    return "设置成功", 0
}
```

**关键概念说明**：
- **禁用时删除会话**：群组被禁用后，用户不应在会话列表看到该群，需同步删除Session记录
- **不解散群组**：禁用与解散不同，禁用后群组数据保留，可重新启用

---

### 3.6 数据库操作说明

| 操作 | 表 | 条件 | 说明 |
|------|-----|------|------|
| UPDATE | GroupInfo | uuid = ? | 更新status字段 |
| UPDATE | Session | receive_id = ? | 仅当status=禁用时执行软删除 |

---

## 四、总结对比

| 接口 | 核心功能 | 关键操作 | Redis缓存 |
|------|---------|---------|-----------|
| GetGroupInfoList | 获取所有群组 | Unscoped()查询含软删除 | 不使用 |
| DeleteGroups | 批量删除群组 | 多表软删除+批量清理 | 清理缓存 |
| SetGroupsStatus | 设置群组状态 | 禁用时同步删除会话 | 注释掉 |

---

## 五、枚举定义

### 群组状态枚举

```go
// pkg/enum/group_info/group_status_enum/group_status_enum.go
package group_status_enum

const (
    NORMAL   int8 = 0  // 正常
    DISABLE  int8 = 1  // 禁用
    DISSOLVE int8 = 2  // 解散
)
```

---

## 六、管理员接口特点

1. **权限验证**：通常需要中间件验证管理员身份
2. **批量操作**：支持同时对多个群组进行操作
3. **全量查询**：使用Unscoped()查询所有记录（含软删除）
4. **谨慎操作**：删除/禁用操作影响面广，需谨慎使用