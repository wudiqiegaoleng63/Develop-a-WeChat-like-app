export interface UserSession {
  session_id: string
  user_id: string
  user_name: string
  avatar: string
}

export interface GroupSession {
  session_id: string
  group_id: string
  group_name: string
  avatar: string
}
